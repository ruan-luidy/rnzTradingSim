<UserControl
  x:Class="rnzTradingSim.Views.SimplePortfolioView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:hc="https://handyorg.github.io/handycontrol"
  xmlns:local="clr-namespace:rnzTradingSim.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:rnzTradingSim.ViewModels"
  d:DataContext="{d:DesignInstance Type=vm:SimplePortfolioViewModel}"
  d:DesignHeight="800"
  d:DesignWidth="1150"
  Background="{StaticResource DarkBackgroundBrush}"
  mc:Ignorable="d">

  <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <Grid Margin="24">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <!--  Header  -->
      <Border Grid.Row="0" Padding="0,0,0,32">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          
          <StackPanel Grid.Column="0">
            <TextBlock
              FontSize="30"
              FontWeight="Bold"
              Foreground="White"
              Text="Portfolio" />
            <TextBlock
              Margin="0,8,0,0"
              FontSize="16"
              Foreground="{DynamicResource SecondaryTextBrush}"
              Text="Manage your investments and transactions" />
          </StackPanel>

          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button
              Height="40"
              Padding="16,0"
              hc:BorderElement.CornerRadius="8"
              Background="{StaticResource RugplayPrimaryBrush}"
              BorderBrush="{StaticResource RugplayPrimaryBrush}"
              Command="{Binding SendMoneyCommand}"
              Foreground="White"
              IsEnabled="{Binding CanSendMoney}">
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="💸" Margin="0,0,8,0" />
                <TextBlock Text="Send Money" />
              </StackPanel>
            </Button>
          </StackPanel>
        </Grid>
      </Border>

      <!--  Loading Indicator  -->
      <Grid 
        Grid.Row="1" 
        Visibility="{Binding IsLoading, Converter={StaticResource Boolean2VisibilityConverter}}"
        Margin="0,0,0,24">
        <hc:LoadingCircle
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          IsRunning="{Binding IsLoading}"
          Style="{StaticResource LoadingCircleLarge}" />
      </Grid>

      <!--  Portfolio Summary Cards  -->
      <Grid 
        Grid.Row="2" 
        Margin="0,0,0,32"
        Visibility="{Binding IsLoading, Converter={StaticResource Boolean2VisibilityReConverter}}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Total Portfolio Value  -->
        <Border
          Grid.Column="0"
          Margin="0,0,8,0"
          Padding="24,20"
          Background="{DynamicResource CardBackgroundBrush}"
          BorderBrush="#16a34a"
          BorderThickness="1"
          CornerRadius="8">
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
              <TextBlock Text="💰" FontSize="14" Margin="0,0,8,0" />
              <TextBlock
                FontSize="14"
                FontWeight="Medium"
                Foreground="{DynamicResource SecondaryTextBrush}"
                Text="Total" />
            </StackPanel>
            <TextBlock
              FontSize="30"
              FontWeight="Bold"
              Foreground="White"
              Text="{Binding TotalPortfolioValueFormatted}" />
          </StackPanel>
        </Border>

        <!--  Cash Balance  -->
        <Border
          Grid.Column="1"
          Margin="8,0"
          Padding="24,20"
          Background="{DynamicResource CardBackgroundBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="1"
          CornerRadius="8">
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
              <TextBlock Text="💵" FontSize="14" Margin="0,0,8,0" />
              <TextBlock
                FontSize="14"
                FontWeight="Medium"
                Foreground="{DynamicResource SecondaryTextBrush}"
                Text="Cash Balance" />
            </StackPanel>
            <TextBlock
              FontSize="30"
              FontWeight="Bold"
              Foreground="White"
              Text="{Binding CashBalanceFormatted}" />
            <TextBlock
              FontSize="12"
              Foreground="{DynamicResource SecondaryTextBrush}"
              Text="{Binding CashPercentageText}" />
          </StackPanel>
        </Border>

        <!--  Coin Holdings Value  -->
        <Border
          Grid.Column="2"
          Margin="8,0,0,0"
          Padding="24,20"
          Background="{DynamicResource CardBackgroundBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="1"
          CornerRadius="8">
          <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
              <TextBlock Text="📈" FontSize="14" Margin="0,0,8,0" />
              <TextBlock
                FontSize="14"
                FontWeight="Medium"
                Foreground="{DynamicResource SecondaryTextBrush}"
                Text="Coin Holdings" />
            </StackPanel>
            <TextBlock
              FontSize="30"
              FontWeight="Bold"
              Foreground="White"
              Text="{Binding CoinHoldingsValueFormatted}" />
            <TextBlock
              FontSize="12"
              Foreground="{DynamicResource SecondaryTextBrush}"
              Text="{Binding HoldingsCountText}" />
          </StackPanel>
        </Border>
      </Grid>

      <!--  Portfolio Content  -->
      <Grid Grid.Row="3" Visibility="{Binding IsLoading, Converter={StaticResource Boolean2VisibilityReConverter}}">
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Holdings or Empty State  -->
        <Border
          Grid.Row="0"
          Background="{DynamicResource CardBackgroundBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="1"
          CornerRadius="8">

          <Grid>
            <!--  Empty State  -->
            <Grid Visibility="{Binding HasHoldings, Converter={StaticResource Boolean2VisibilityReConverter}}">
              <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="0,64">
                <Border
                  Width="64"
                  Height="64"
                  Margin="0,0,0,16"
                  HorizontalAlignment="Center"
                  Background="{DynamicResource SecondaryRegionBrush}"
                  CornerRadius="32">
                  <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="24"
                    Text="💰" />
                </Border>
                <TextBlock
                  HorizontalAlignment="Center"
                  FontSize="18"
                  FontWeight="SemiBold"
                  Foreground="White"
                  Text="No coin holdings" />
                <TextBlock
                  Margin="0,8,0,24"
                  HorizontalAlignment="Center"
                  FontSize="14"
                  Foreground="{DynamicResource SecondaryTextBrush}"
                  Text="You haven't invested in any coins yet. Start by buying existing coins." />
                <Button
                  Padding="20,12"
                  hc:BorderElement.CornerRadius="8"
                  Background="{DynamicResource RegionBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  Command="{Binding BrowseCoinsCommand}"
                  Content="Browse Coins"
                  Foreground="White" />
              </StackPanel>
            </Grid>

            <!--  Holdings Table  -->
            <Grid Visibility="{Binding HasHoldings, Converter={StaticResource Boolean2VisibilityConverter}}">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Table Header  -->
            <Border Grid.Row="0" Padding="24,20">
              <StackPanel>
                <TextBlock
                  FontSize="18"
                  FontWeight="SemiBold"
                  Foreground="White"
                  Text="Your Holdings" />
                <TextBlock
                  Margin="0,4,0,0"
                  FontSize="14"
                  Foreground="{DynamicResource SecondaryTextBrush}"
                  Text="Current positions in your portfolio" />
              </StackPanel>
            </Border>

            <!--  Holdings Data Table  -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
              <DataGrid
                Padding="24,0,24,24"
                Background="Transparent"
                BorderBrush="Transparent"
                BorderThickness="0"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="False"
                CanUserResizeColumns="False"
                CanUserResizeRows="False"
                CanUserSortColumns="True"
                GridLinesVisibility="None"
                HeadersVisibility="Column"
                IsReadOnly="True"
                ItemsSource="{Binding Holdings}"
                RowBackground="Transparent"
                SelectionMode="Single"
                MouseDoubleClick="HoldingsDataGrid_MouseDoubleClick">
                
                <DataGrid.RowStyle>
                  <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                    <Setter Property="Height" Value="60" />
                    <Style.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
                        <Setter Property="Cursor" Value="Hand" />
                      </Trigger>
                    </Style.Triggers>
                  </Style>
                </DataGrid.RowStyle>

                <DataGrid.ColumnHeaderStyle>
                  <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="FontWeight" Value="Medium" />
                    <Setter Property="Padding" Value="0,0,0,12" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                  </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                  <!--  Coin Column  -->
                  <DataGridTemplateColumn Header="Coin" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                          <Border
                            Width="24"
                            Height="24"
                            Margin="0,0,8,0"
                            Background="{StaticResource RugplayPrimaryBrush}"
                            CornerRadius="12">
                            <TextBlock
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              FontSize="10"
                              FontWeight="Bold"
                              Foreground="White"
                              Text="{Binding Symbol}" />
                          </Border>
                          <TextBlock
                            FontSize="14"
                            FontWeight="Medium"
                            Foreground="White"
                            Text="{Binding CoinName}" />
                        </StackPanel>
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>

                  <!--  Quantity Column  -->
                  <DataGridTextColumn 
                    Header="Quantity" 
                    Binding="{Binding QuantityFormatted}"
                    Width="120">
                    <DataGridTextColumn.ElementStyle>
                      <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="FontFamily" Value="Consolas" />
                        <Setter Property="Foreground" Value="White" />
                      </Style>
                    </DataGridTextColumn.ElementStyle>
                  </DataGridTextColumn>

                  <!--  Price Column  -->
                  <DataGridTextColumn 
                    Header="Price" 
                    Binding="{Binding CurrentPriceFormatted}"
                    Width="100">
                    <DataGridTextColumn.ElementStyle>
                      <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="FontFamily" Value="Consolas" />
                        <Setter Property="Foreground" Value="White" />
                      </Style>
                    </DataGridTextColumn.ElementStyle>
                  </DataGridTextColumn>

                  <!--  P&L % Column  -->
                  <DataGridTemplateColumn Header="P&amp;L %" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
                        <Border
                          Padding="8,4"
                          CornerRadius="4">
                          <Border.Style>
                            <Style TargetType="Border">
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPositivePnL}" Value="True">
                                  <Setter Property="Background" Value="#16a34a" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsPositivePnL}" Value="False">
                                  <Setter Property="Background" Value="#dc2626" />
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </Border.Style>
                          <TextBlock
                            FontSize="12"
                            FontWeight="Medium"
                            Foreground="White"
                            Text="{Binding PnLPercentageFormatted}" />
                        </Border>
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>

                  <!--  24h Change Column  -->
                  <DataGridTemplateColumn Header="24h Change" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
                        <Border
                          Padding="8,4"
                          CornerRadius="4">
                          <Border.Style>
                            <Style TargetType="Border">
                              <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPositive24hChange}" Value="True">
                                  <Setter Property="Background" Value="#16a34a" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsPositive24hChange}" Value="False">
                                  <Setter Property="Background" Value="#dc2626" />
                                </DataTrigger>
                              </Style.Triggers>
                            </Style>
                          </Border.Style>
                          <TextBlock
                            FontSize="12"
                            FontWeight="Medium"
                            Foreground="White"
                            Text="{Binding Change24hFormatted}" />
                        </Border>
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>

                  <!--  Value Column  -->
                  <DataGridTextColumn 
                    Header="Value" 
                    Binding="{Binding ValueFormatted}"
                    Width="120">
                    <DataGridTextColumn.ElementStyle>
                      <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="FontFamily" Value="Consolas" />
                        <Setter Property="FontWeight" Value="Medium" />
                        <Setter Property="Foreground" Value="White" />
                      </Style>
                    </DataGridTextColumn.ElementStyle>
                  </DataGridTextColumn>

                  <!--  Portfolio % Column  -->
                  <DataGridTemplateColumn Header="Portfolio %" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
                        <Border
                          Padding="8,4"
                          Background="{DynamicResource SecondaryRegionBrush}"
                          CornerRadius="4">
                          <TextBlock
                            FontSize="12"
                            FontWeight="Medium"
                            Foreground="White"
                            Text="{Binding PortfolioPercentageFormatted}" />
                        </Border>
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>
                </DataGrid.Columns>
              </DataGrid>
            </ScrollViewer>
            </Grid>
          </Grid>
        </Border>

        <!--  Recent Transactions  -->
        <Border
          Grid.Row="1"
          Margin="0,32,0,0"
          Background="{DynamicResource CardBackgroundBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="1"
          CornerRadius="8">
          
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Transactions Header  -->
            <Border Grid.Row="0" Padding="24,20">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🧾" FontSize="16" Margin="0,0,8,0" />
                    <TextBlock
                      FontSize="18"
                      FontWeight="SemiBold"
                      Foreground="White"
                      Text="Recent Transactions" />
                  </StackPanel>
                  <TextBlock
                    Margin="0,4,0,0"
                    FontSize="14"
                    Foreground="{DynamicResource SecondaryTextBrush}"
                    Text="Your latest trading activity" />
                </StackPanel>

                <Button
                  Grid.Column="1"
                  Padding="12,8"
                  hc:BorderElement.CornerRadius="6"
                  Background="{DynamicResource RegionBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  Command="{Binding ViewAllTransactionsCommand}"
                  Content="View All"
                  FontSize="12"
                  Foreground="White"
                  Visibility="{Binding HasTransactions, Converter={StaticResource Boolean2VisibilityConverter}}" />
              </Grid>
            </Border>

            <!--  Transactions Table (Simplified)  -->
            <Border Grid.Row="1" Padding="24,0,24,24" MinHeight="200">
              <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="14"
                Foreground="{DynamicResource SecondaryTextBrush}"
                Text="No transactions yet" 
                Visibility="{Binding HasTransactions, Converter={StaticResource Boolean2VisibilityReConverter}}" />
            </Border>
          </Grid>
        </Border>
      </Grid>

    </Grid>
  </ScrollViewer>
</UserControl>